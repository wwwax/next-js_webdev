import { Heading } from '@/components/Heading';

const Error = () => {
  return (
    <div>
      <Heading text="404" />
      <Heading text="Error: Something went wrong" />
    </div>
  );
};

export default Error;
